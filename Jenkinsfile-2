pipeline {
  agent { label 'agent1' }

  environment{
    MY_SECRETS = credentials('secrets')
  }
  stages {
    stage('Copy cred data') {
      steps {
        sh '''
            echo $MY_SECRETS >> test.txt
        '''
      }
    }
  }
}
