pipeline {
  agent { label 'agent1' }

  environment{
    MY_TOKEN = credentials('yc_token')
  }
  stages {
    stage('Copy cred data') {
      steps {
        sh '''
            echo $MY_TOKEN >> test.txt
        '''
      }
    }
  }
}