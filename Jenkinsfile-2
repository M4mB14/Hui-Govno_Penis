pipeline {
  agent { label 'agent1' }

  environment{
    MY_TOKEN = credentials('52')
  }
  stages {
    stage('Test') {
      steps {
        sh '''
            echo $MY_TOKEN
        '''
      }
    }
  }
}
