pipeline {
  agent { label 'agent1' }
  
  environmen{
    MY_TOKEN = credentials(52)
  }
  stages {
    stage('Test') {
      steps {
        sh '''
            echo $MY_TOKEN
        '''
      }
    }
  }
}
