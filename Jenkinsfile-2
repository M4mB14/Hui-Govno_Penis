pipeline {
  agent { label 'agent1' }

  stages {
    stage('ssh key generate') {
      steps {
        sh '''
        ssh-keygen -t rsa -b 4096 -C "jenkins@ci" -N "" -f id_rsa 
        '''
      }
    }
  }
}